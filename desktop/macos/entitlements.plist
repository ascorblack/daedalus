<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  The launcher asks for nothing beyond what a program is given by default: it reads and writes the
  folder it was put in, talks to the loopback address, and runs the docker client that is already
  installed. The dictionary is empty on purpose. It exists because notarization requires the
  hardened runtime, and codesign takes the runtime and the entitlements together.
-->
<plist version="1.0">
<dict/>
</plist>
